# UI Components

Reusable, presentational components like buttons, inputs, cards, etc.

These components should be:
- Highly reusable across the application
- Presentation-focused (minimal business logic)
- Well-typed with TypeScript
- Documented with JSDoc comments

Example: `Button.tsx`, `Input.tsx`, `Card.tsx`, `Badge.tsx`
