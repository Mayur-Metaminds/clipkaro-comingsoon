# Feature Components

Feature-specific, composed components with business logic.

Organize by feature domain:
- `auth/` - Authentication-related components
- `dashboard/` - Dashboard-specific components
- `profile/` - User profile components

These components can use UI components and contain feature-specific logic.
